# Exploring the List Interface

## 1. What is a `List`?
- A `List` in Java is like a **grocery list** 📜.
- It **remembers the order** in which elements were added.
- Each item has an **index (starting from 0)**.

### Example: Creating a List
```java
import java.util.*;

public class ListExample {
    public static void main(String[] args) {
        List<String> names = new ArrayList<>();
        names.add("Alice");
        names.add("Bob");
        names.add("Charlie");

        System.out.println(names); // [Alice, Bob, Charlie]
        System.out.println(names.get(1)); // Bob (index starts from 0)
    }
}
2. ArrayList vs. LinkedList (Which One to Use?)
Feature	ArrayList (Bookshelf 📚)	LinkedList (Train 🚆)
Fast access	✅ Yes (direct access)	❌ No (must move from start)
Fast insert/remove	❌ No (shifting needed)	✅ Yes (just re-link nodes)
Best for?	Reading (getting items fast)	Frequent insert/delete
Example: Performance Comparison
java
Copy
Edit
import java.util.*;

public class ListPerformance {
    public static void main(String[] args) {
        List<Integer> arrayList = new ArrayList<>();
        List<Integer> linkedList = new LinkedList<>();

        for (int i = 0; i < 1_000_000; i++) {
            arrayList.add(i);
            linkedList.add(i);
        }

        long start = System.nanoTime();
        arrayList.get(500_000);
        System.out.println("ArrayList get() time: " + (System.nanoTime() - start) + " ns");

        start = System.nanoTime();
        linkedList.get(500_000);
        System.out.println("LinkedList get() time: " + (System.nanoTime() - start) + " ns");
    }
}
3. Adding, Removing, and Updating Items
Example: Basic Operations
java
Copy
Edit
import java.util.*;

public class ListOperations {
    public static void main(String[] args) {
        List<String> list = new ArrayList<>(Arrays.asList("A", "B", "C", "D"));

        list.add(2, "X"); // Insert at index 2
        System.out.println(list); // [A, B, X, C, D]

        list.set(3, "Y"); // Replace index 3
        System.out.println(list); // [A, B, X, Y, D]

        list.remove(1); // Remove index 1
        System.out.println(list); // [A, X, Y, D]
    }
}
4. Finding Items in the List
Example: Finding Index of an Item
java
Copy
Edit
import java.util.*;

public class ListSearch {
    public static void main(String[] args) {
        List<String> colors = Arrays.asList("Red", "Green", "Blue", "Red");

        System.out.println(colors.indexOf("Red"));  // First Red → 0
        System.out.println(colors.lastIndexOf("Red"));  // Last Red → 3
    }
}
5. Getting a SubList
Example: Extracting Part of a List
java
Copy
Edit
import java.util.*;

public class SubListExample {
    public static void main(String[] args) {
        List<String> list = new ArrayList<>(Arrays.asList("A", "B", "C", "D", "E"));
        
        List<String> sublist = list.subList(1, 4);
        System.out.println(sublist); // [B, C, D]

        sublist.set(0, "X");
        System.out.println(list); // [A, X, C, D, E] (Original list is also changed!)
    }
}
6. Sorting a List
Example: Sorting Numbers
java
Copy
Edit
import java.util.*;

public class SortingExample {
    public static void main(String[] args) {
        List<Integer> numbers = new ArrayList<>(Arrays.asList(5, 2, 8, 1, 3));

        numbers.sort(Comparator.naturalOrder()); // Ascending order
        System.out.println(numbers); // [1, 2, 3, 5, 8]

        numbers.sort(Comparator.reverseOrder()); // Descending order
        System.out.println(numbers); // [8, 5, 3, 2, 1]
    }
}
7. Iterating (Looping) Through a List
Example: Changing Values While Looping
java
Copy
Edit
import java.util.*;

public class ListIteratorExample {
    public static void main(String[] args) {
        List<String> list = new ArrayList<>(Arrays.asList("one", "two", "three"));

        ListIterator<String> iterator = list.listIterator();

        while (iterator.hasNext()) {
            String value = iterator.next();
            if (value.equals("two")) {
                iterator.set("2");
            }
        }

        System.out.println(list); // [one, 2, three]
    }
}
Example: Looping Backward
java
Copy
Edit
import java.util.*;

public class BackwardIteration {
    public static void main(String[] args) {
        List<String> list = Arrays.asList("A", "B", "C");

        ListIterator<String> iterator = list.listIterator(list.size());

        while (iterator.hasPrevious()) {
            System.out.println(iterator.previous());
        }
        // Output: C B A
    }
}
Summary
✅ List remembers order and lets you access items by index.
✅ Use ArrayList if you mostly read data.
✅ Use LinkedList if you insert/delete data often.
✅ You can add, remove, update, and search items easily.
✅ You can sort and loop through a list in different ways.
